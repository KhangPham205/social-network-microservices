# Normalize line endings: LF in the repo and in every checkout (project is developed on Windows and Linux)
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.png binary
*.jpg binary
*.jar binary
